version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building MAJU backend Docker image"
run:
  runtime-version: docker
  network:
    port: 3001
    env: PORT
  env:
    - name: PORT
      value: "3001"
